// Point this at your running backend (see /backend). The browser never talks
// to Snowflake directly - it only calls this API, which holds the credentials.
window.API_BASE_URL = "http://localhost:4000";
